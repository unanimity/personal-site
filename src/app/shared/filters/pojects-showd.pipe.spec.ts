import { PojectsShowdPipe } from './pojects-showd.pipe';

describe('PojectsShowdPipe', () => {
  it('create an instance', () => {
    const pipe = new PojectsShowdPipe();
    expect(pipe).toBeTruthy();
  });
});
